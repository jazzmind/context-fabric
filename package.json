{
  "name": "context-fabric",
  "version": "0.2.0",
  "private": true,
  "description": "Deterministic context runtime for coding agents: task-scoped versioned packs optimized for attention quality and prefix-cache reuse.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "python3 -m unittest discover -s tests -p \"test_*.py\""
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
