---
description: Check, enable ("local <oMLX model id>" or "cloud <provider> <model id>"), or disable ("off") the research lane — a separate model for tangential lookups kept off the primed quality-lane session.
agent: build
---
!`python3 scripts/context_research_lane.py "$ARGUMENTS"`
